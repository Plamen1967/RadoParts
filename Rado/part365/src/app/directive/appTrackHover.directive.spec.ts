import { TrackHoverDirective } from './appTrackHover.directive';

describe('Directive: AppTrackHover', () => {
  it('should create an instance', () => {
    const directive = new TrackHoverDirective();
    expect(directive).toBeTruthy();
  });
});
