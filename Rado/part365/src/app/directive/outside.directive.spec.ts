import { OutsideDirective } from './outside.directive';

describe('Directive: Outside', () => {
  it('should create an instance', () => {
    const directive = new OutsideDirective();
    expect(directive).toBeTruthy();
  });
});
